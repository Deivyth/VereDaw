{% extends 'index.html.twig' %}

{% block body %}
    {# {{solicitudes.equipo.nombre}} #}
     {% for solicitud in solicitudes %}
        {{solicitud.equipo.nombre}} 
        {{solicitud.usuario.nombre}} 
        <a class="btn btn-primary" onclick="addUserToTeam()">Añadir</a>
    {% endfor %} 
{% endblock %}

<script>
    function addUserToTeam(){
        console.log({{solicitud.equipo.nombre}});
    //var data = new FormData();
    //data.append("equipo", {{solicitud.equipo.nombre}})
    }

</script>